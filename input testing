pipeline {
  agent none
    stages { 
        stage ('input') {
          steps {
            input (message: 'Hello Phincon', ok: 'Submit')
            sh '''
                hostname
              '''
                }
          }
    }
}
